#!/usr/bin/env ruby

require 'pp'
require 'smart_parser'

def usage
  puts "Usage: #{File.basename(__FILE__)} drive"
  exit 1
end

ARGV.count != 1 && usage

parser = SMARTParser.new(ARGV[0])
pp parser.process
